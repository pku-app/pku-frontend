import { StyleSheet } from 'react-native';

export const Style = StyleSheet.create({
    footer: {
        flexDirection: 'row',
        justifyContent: 'space-around'
    },
    footerBtn: {
        alignItems: 'center',
    },
});